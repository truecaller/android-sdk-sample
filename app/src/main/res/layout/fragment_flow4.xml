<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/ic_bg_flow4"
    tools:context="com.truecaller.android.sdksample.Flow4Fragment">

    <FrameLayout
        android:id="@+id/homeLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="40dp">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/truecaller_logo"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:layout_gravity="center_horizontal"
            android:src="@drawable/ic_truecaller_logo_white"
            android:tint="@color/tcBlue"
            tools:ignore="ContentDescription" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/getStartedBtn"
            style="@style/DefaultButton"
            android:layout_gravity="bottom"
            android:layout_marginBottom="50dp"
            android:backgroundTint="@color/white"
            android:gravity="center"
            android:text="Get Started"
            android:textColor="@color/lightBlue"
            android:textSize="16sp" />

    </FrameLayout>

    <include layout="@layout/flow4_layouts"/>

</FrameLayout>
